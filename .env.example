# Optional. Model API keys can also be stored in the local SQLite database.
DEEPSEEK_API_KEY=
DASHSCOPE_API_KEY=

# Optional. Required only when using the "同步到人才库" button on a completed run.
# SUPABASE_URL can be the project URL, for example https://xxxx.supabase.co.
# Alternatively set SUPABASE_REST_URL to the full candidate_talent REST endpoint.
SUPABASE_URL=
SUPABASE_REST_URL=
SUPABASE_SERVICE_ROLE_KEY=
